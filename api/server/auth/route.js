const express = require('express');
const authCtrl = require('./controller');
const router = express.Router(); 

//router.route('/google').post(authCtrl.oAuth);
//router.route('/facebook').post(authCtrl.oAuth);

module.exports = router;
